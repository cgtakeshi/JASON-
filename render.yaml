services:
  - type: web
    runtime: static
    name: jason-game-art-portfolio
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm build:cloudflare
    staticPublishPath: ./deploy/cloudflare-pages
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
